# https://moonrepo.dev/docs/config/workspace
$schema: './cache/schemas/workspace.json'

# extends: './shared/workspace.yml'

projects:
  globs:
  - 'apps/**/moon.yml'
  - 'packages/**/moon.yml'

vcs:
  manager: 'git'
  defaultBranch: 'main'
generator:
  templates:
   - './templates'
   - 'git://github.com/riemannulus/moonrepo-templates#main'
